apiVersion: v1
kind: Service
metadata:
  name: tp-back-app-svc
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: tp-back-app
  ports:
    - port: 3001
      targetPort: 3001
      nodePort: 310002
